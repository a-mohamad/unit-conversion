package unitconversion.model.command;

/**
 * The interface Command used for the Command Design Pattern.
 */
public interface Command {
    /**
     * Execute is the method that needs to be created to execute the command.
     */
    void execute();
}
