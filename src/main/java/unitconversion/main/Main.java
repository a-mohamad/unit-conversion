package unitconversion.main;

import unitconversion.view.MainView;

/**
 * The type Main.
 */
public class Main {
    /**
     * The entry point of application, from where it can be accessed and ran.
     *
     * @param args the input arguments to our application, however in this case they are unused.
     */
    public static void main(String[] args) {
        new MainView();
    }
}
